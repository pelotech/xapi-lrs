# xAPI 2.0 Phase 2B Result — battery fully green

Recorded 2026-07-13 against suite commit
`5bc232d349c60faded8240da698f195106091638` (pinned in `package.json`), branch
`xapi-2.0-negotiation`. This document records the completion of Phase 2B and
**supersedes the "10 failures" framing** of
`docs/superpowers/plans/2026-07-12-xapi-2.0-negotiation-baseline.md`: all 10
v2_0 conformance failures are closed and the battery executes fully green.

## Headline

- **v2_0 ADL battery: 1435 / 1435 passing, 0 failing, 0 pending** (was 1425
  passing / 10 failing at the 2B baseline).
- **1.0.3 ADL battery: 37 / 37** (regression gate held throughout).
- Green on **both schema sources** (migration and lrsql takeover) and both
  drivers (pglite locally; pg + pglite in CI).
- **The 2.0 CI conformance cells are now blocking** (`continue-on-error`
  removed).

## Full green gate — 2026-07-13, `DATABASE_DRIVER=pglite`

Every run below was executed verbatim and recorded.

| Gate                                                                | Result               |
| ------------------------------------------------------------------- | -------------------- |
| `pnpm typecheck && pnpm test`                                       | 232 / 232 passing    |
| `pnpm test:integration`                                             | 57 passed, 5 skipped |
| `pnpm test:conformance` (1.0.3, migration)                          | 37 / 37              |
| `pnpm test:conformance:2.0` (2.0, migration)                        | 38 / 38 (sections)   |
| `SCHEMA_SOURCE=lrsql pnpm test:conformance` (1.0.3, takeover)       | 37 / 37              |
| `SCHEMA_SOURCE=lrsql pnpm test:conformance:2.0` (2.0, **takeover**) | 38 / 38 (sections)   |

Authoritative ADL CLI count:

```text
DATABASE_DRIVER=pglite npx tsx test/conformance/run-adl-suite.ts --xapi-version=2.0.0

=== ADL Conformance Suite Results (LRS) ===
  Version:  2.0.0
  State:    finished
  Total:    1435
  Passing:  1435
  Failing:  0
  Pending:  0
  Duration: 28.0s
```

The per-section vitest wrapper reports 38 section tests (34 mapped top-level
sections plus the three coverage guards, minus the genuinely-absent
`mayBeAbsent` Headers section); the authoritative leaf-test count is the ADL
CLI's **1435 / 1435**.

## 10 → 0 failure closure, mapped to the Phase 2B tasks

| Baseline # | Section                                | Design | Task | Fix                                                                                                                                                                            |
| ---------- | -------------------------------------- | ------ | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1–4        | Context Property (Data 2.4.6)          | 2d     | 1–2  | Accept `context.contextAgents` / `contextGroups` (statement + substatement) for 2.0 requests; decompose them into `statement_to_actor` under `Context*` / `SubContext*` usages |
| 5          | Version Property (Data 2.4.10)         | 2d     | 1    | Accept the `2.0.x` statement `version` property on 2.0 requests, stored as received; `1.0.x`-only rule preserved for 1.0 requests                                              |
| 6          | Statement Resource (Communication 2.1) | 2e     | 3    | Add `Last-Modified` header (matching `stored` to the second) to statement GET responses                                                                                        |
| 7          | Statement Resource (Communication 2.1) | 2f     | 4    | Reject a batch containing duplicate statement `id`s with `400` before any insert                                                                                               |
| 8–10       | (4.1.4) Concurrency                    | 2h     | 5    | Enforce State-resource ETag concurrency for 2.0: PUT to an existing doc without a precondition → `409` + error body, resource unmodified (1.0.3 and new-doc PUT stay `204`)    |

All Chunk-2 fixes are schema-agnostic, and contextAgents/contextGroups
decompose into `statement_to_actor` (present in both the migration and lrsql
schemas), which is why the **2.0 lrsql-takeover** run is green as well — the
exact cells being made blocking were verified before flipping CI.

## CI gating change

`.github/workflows/ci.yml` `conformance-tests` job:

- Removed `continue-on-error: ${{ matrix.xapi-version == '2.0.0' }}` (and its
  now-stale "expected to fail until 2.0-conformant" comment). The 2.0
  conformance cells are now required.
- Removed the `exclude` of `xapi-version: '2.0.0' + schema-source: lrsql`
  (whose justification was that those cells were "already red-by-default").
  With the 2.0 takeover battery green, the matrix now runs — and requires —
  all four 2.0 cells: migration + lrsql × pg + pglite.

## Metadata refresh

- `test/conformance/spec-sections.ts`: `TOTAL_FLOOR['2.0.0']` stays `1363`
  (~95% of 1435; the executed total did not move). Comment updated to record
  the fully-green result (1435 passing / 0 failing / 0 pending) on both
  schemas.
- `test/conformance/adl-conformance.test.ts`: top-of-file comment updated from
  "four sections still red pending Phase 2B" to "now fully green."

## Exit criteria — met

- v2_0 ADL battery: 0 failing (was 10), on both drivers and both schema sources.
- contextAgents/contextGroups + 2.0.x version accepted for 2.0, rejected for 1.0.
- Context actors decomposed into `statement_to_actor` (`Context*` / `SubContext*`).
- Statement GET carries a `Last-Modified` header matching `stored` to the second.
- Same-`id` batches rejected `400`.
- State PUT to an existing doc without a precondition → `409` (2.0 only; 1.0.3
  and new-doc PUT stay `204`).
- 1.0.3 battery still 37/37, takeover-mode conformance still 37/37.
- 2.0 CI conformance cells flipped to blocking.

xAPI 2.0 conformance is complete.
