apiVersion: v2
name: xapi-lrs
description: xapi-lrs Learning Record Store helm chart
type: application
home: https://github.com/pelotech/xapi-lrs
sources:
  - https://github.com/pelotech/xapi-lrs
keywords:
  - xapi
  - lrs
  - learning-record-store
  - tin-can

# Chart version. Deliberately kept in LOCKSTEP with appVersion below: this chart
# exists only to deploy this application, and release-please drives both from the
# same release (see release-please-config.json). Do not edit by hand, and do not
# split them back apart without reading the note in the workflow — two packages
# releasing each other is what produced the release loop.
version: 0.9.7 # x-release-please-version

# This is the version number of the application being deployed. This version number should be
# incremented each time you make changes to the application. Versions are not expected to
# follow Semantic Versioning. They should reflect the version the application is using.
# It is recommended to use it with quotes.
appVersion: '0.9.7' # x-release-please-version
