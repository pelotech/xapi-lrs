{{- /*
  This README is generated. Both versions below come from Chart.yaml, which
  release-please keeps current (chart version on a chart release, appVersion on
  an app release), and the chart-docs job in .github/workflows/release-please.yml
  regenerates this file on the release PR branch.

  Do NOT add x-release-please-version markers here. The generic updater rewrites
  every marked line in a file, so with the app package and the chart package
  both owning lines in this README each release would stamp its version over the
  other's — and because only the first version on a line is replaced, the
  appVersion badge would end up with mismatched alt text and image URL.
*/ -}}
{{ template "chart.header" . }}

### version: {{ template "chart.version" . }}

{{ template "chart.appVersionBadge" . }}

{{ template "chart.description" . }}

{{ template "chart.homepageLine" . }}

## Installing

```shell
helm install xapi-lrs oci://ghcr.io/pelotech/xapi-lrs/charts/xapi-lrs
```

{{ template "chart.requirementsSection" . }}

{{ template "chart.valuesSection" . }}

{{ template "helm-docs.versionFooter" . }}
